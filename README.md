# YouTube Top Comment API Test (Mock)

Automation QA test using mocked YouTube API response.
Test validates parsing, identifies top liked comment, saves to file.

## How to run

1. Activate virtualenv
2. `pip install -r requirements.txt`
3. `python get_top_comment_mock.py`

Result: `top_comment.txt` is created.